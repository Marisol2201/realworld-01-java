/*import org.junit.jupiter.api.Test;

import java.time.LocalDate;
import java.time.Month;

import static org.junit.jupiter.api.Assertions.*;

public class BankStatementProcessorTest {

    @Test
    public void shouldAcceptValidDate() throws Exception {
        String date1 = "30-01-2017";
        String date2 = "21-05-2019";

        calculateMaxTransaction expected = new calculateMaxTransaction(
                date1, date2
        );
        double tolerance = 0.0d;

        assertEquals(expected.getDate(), result.getDate());
        assertEquals(expected.getAmount(), result.getAmount(), tolerance);
        assertEquals(expected.getDescription(), result.getDescription());

    }
}
*/